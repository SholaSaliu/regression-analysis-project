# Regression Analysis Project

## Overview
This project performs a comprehensive regression analysis to explore relationships between key variables within the dataset. It leverages standard statistical models to extract insights and visualize trends.

## Environment Setup
To run this Jupyter Notebook locally, you need to install the required Python libraries. Run the following command in your terminal or command prompt:

```bash
pip install pandas seaborn statsmodels jupyter
